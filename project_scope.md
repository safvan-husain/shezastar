## Vehicle Electronic Gadgets E- commerce.

As admin I should able to updaload the product with multiple images, variant.

the website should collect, multiple images, nonOfferPrice?, price,
when added variant, have price for each variant,
for each product add the option to create variant type (like color, size, storage, ect..) since it will be dynamic, so first let me create the variant type, and add items to each variant, later when uploaded the image, I should able to drag, either the combination, or a specific varaint items to the product image, so that later when user select variant options, I can filter the image.

for now do not have admin/user login, just have the admin panel for creating, updating, deleteting, 